# Bloqueia acesso web direto a esta pasta (o app inclui via filesystem).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
